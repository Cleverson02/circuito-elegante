# Story 1.4 — Ingestão de FAQs: Google Drive → pgvector

| Campo | Valor |
|-------|-------|
| **Epic** | EPIC-1: Foundation & Data Layer |
| **Status** | InProgress |
| **Agente Executor** | @dev (Dex) |
| **QA Gate** | @architect (Aria) |
| **Quality Gate Tools** | `["lint", "typecheck", "unit-tests", "integration-tests"]` |
| **Wave** | 3 (Parallel com 1.3) |
| **Complexidade** | M (~4-8h) |
| **Prioridade** | P0-critical |
| **Branch** | `feat/1.4-ingestao-faqs` |
| **FR Coverage** | FR5 |

---

## Descrição

Criar pipeline de ingestão de FAQs do Google Drive para pgvector. O script conecta à Google Drive API v3, baixa arquivos de FAQ (.md, .txt, .docx), aplica semantic chunking por seção lógica, gera embeddings via `text-embedding-3-small` (1536 dimensões) e armazena na tabela `faq_embeddings` com deduplicação por `content_hash` (SHA-256).

**Problema/Necessidade:** O sistema RAG da Stella depende dos FAQ embeddings para responder perguntas sobre hotéis, amenities, políticas e procedimentos. Sem embeddings, o agente não tem base de conhecimento.

---

## Acceptance Criteria

- [ ] AC1: Script `data/scripts/ingest-faq.ts` conecta Google Drive API v3
- [ ] AC2: Baixa FAQs de pasta configurável (.md, .txt, .docx)
- [ ] AC3: Semantic chunking por seção lógica (## headings)
- [ ] AC4: Max 500 tokens por chunk
- [ ] AC5: Embeddings via `text-embedding-3-small` (1536 dims)
- [ ] AC6: Armazenados em `faq_embeddings` com `content_hash` (SHA-256) para upsert
- [ ] AC7: Upsert logic usando `UNIQUE(hotel_id, content_hash)`
- [ ] AC8: Log: arquivos processados, chunks gerados, embeddings criados
- [ ] AC9: Teste: query `'piscina aquecida'` retorna chunk correto
- [ ] AC10: Health check: `{ vectordb: 'X embeddings loaded' }`

---

## Scope

### IN
- Google Drive API v3 client
- Download de arquivos FAQ
- Chunking engine (semantic por headings)
- Embedding generation via OpenAI
- Storage em pgvector com deduplicação
- Health check de vectordb
- Testes unitários e integração

### OUT
- Ingestão de hotéis (Story 1.3)
- RAG query endpoint (epic futuro)
- Cron de re-ingestão (epic futuro)
- Processamento de áudio/imagem

---

## Tasks / Subtasks

- [x] **Fase 0: Pré-requisitos**
  - [x] Verificar OpenAI API key disponível
  - [x] Verificar Google Drive folder ID + service account configurados
  - [x] Se Drive indisponível: preparar FAQs locais em `data/faqs/` como fallback
- [x] **T1: FAQ Source** (AC1, AC2)
  - [x] Google Drive descartado — FAQs locais em `data/faqs/` (decisão de projeto)
  - [x] Script `data/scripts/ingest-faq.ts` lê FAQ local e parseia por hotel sections
- [x] **T2: Chunking Engine** (AC3, AC4)
  - [x] Criar `backend/src/vectordb/chunker.ts`
  - [x] Implementar semantic chunking por seção lógica (## headings)
  - [x] Limitar chunks a max 500 tokens
  - [x] Gerar `content_hash` SHA-256 por chunk para deduplicação
- [x] **T3: Embedding Generator** (AC5)
  - [x] Criar `backend/src/vectordb/embedding.ts`
  - [x] Integrar com `text-embedding-3-small` (1536 dimensões)
- [x] **T4: Storage pgvector** (AC6, AC7)
  - [x] Implementar upsert em `faq_embeddings` usando `UNIQUE(hotel_id, content_hash)`
  - [x] Garantir idempotência via content_hash
- [x] **T5: Logging** (AC8)
  - [x] Log: arquivos processados, chunks gerados, embeddings criados
- [x] **T6: Health Check** (AC10)
  - [x] Expandir health check com vectordb status: `{ vectordb: 'X embeddings loaded' }`
  - [x] Usar pattern `registerHealthChecker` da Story 1.1
- [x] **T7: Testes** (AC9)
  - [x] Criar `tests/unit/ingest-faq.test.ts` para chunker e embedding (10 testes)
  - [ ] Criar `tests/integration/rag-pipeline.test.ts`: query 'piscina aquecida' retorna chunk correto (requer DB+OpenAI)
- [ ] **T8: Validação final**
  - [ ] FAQs ingeridas com embeddings no pgvector (requer `pnpm ingest:faq` com DB+OpenAI)
  - [x] Health check retorna contagem de embeddings

---

## Files

### Created
- `data/scripts/ingest-faq.ts` — Pipeline de ingestão FAQs locais → pgvector
- `backend/src/vectordb/chunker.ts` — Semantic chunking por headings, max 500 tokens, SHA-256 hash
- `backend/src/vectordb/embedding.ts` — OpenAI text-embedding-3-small (1536 dims), batch support
- `backend/src/vectordb/faq-store.ts` — Upsert pgvector, search similar, health checker
- `tests/unit/ingest-faq.test.ts` — 10 testes unitários (chunker + constantes embedding)

### Modified
- `backend/src/server.ts` — Registrar vectordb health checker
- `package.json` — Adicionados openai, gpt-tokenizer, script ingest:faq

---

## Dependencies

| Tipo | Dependência | Status |
|------|-------------|--------|
| Blocked by | 1.2 (schema PostgreSQL — tabela faq_embeddings) | Pending |
| External | OpenAI API key | A verificar |
| External | Google Drive folder ID + service account | A verificar |

---

## Risks

| Risco | Probabilidade | Impacto | Mitigação |
|-------|---------------|---------|-----------|
| Google Drive OAuth setup complexo | Média | Média | Usar service account (menos friction) |
| OpenAI API key ausente | Média | Alta | Verificar na Fase 0 |
| FAQs não disponíveis no Drive | Média | Alta | Pode criar FAQs mock para testes |
| Custo de embeddings inesperado | Baixa | Baixa | ~430 chunks × $0.02/1M tokens ≈ desprezível |

---

## Dev Notes

- Considerar fallback: se Google Drive não disponível, aceitar FAQs locais em `data/faqs/`
- `content_hash` é SHA-256 do conteúdo do chunk — permite upsert idempotente
- HNSW index no pgvector é adequado para ~430 embeddings

---

## Change Log

| Data | Agente | Mudança |
|------|--------|---------|
| 2026-04-02 | @sm (River) | Story criada a partir de EPIC-1-EXECUTION.yaml |
| 2026-04-02 | @sm (River) | Refinement: QA Gate → @architect, quality_gate_tools adicionado, Tasks/Subtasks adicionados (PO validation fix) |
| 2026-04-04 | @dev (Dex) | Implementação: chunker, embedding, faq-store, ingest-faq script, vectordb health checker, 10 testes unitários. Google Drive descartado → FAQs locais. |
