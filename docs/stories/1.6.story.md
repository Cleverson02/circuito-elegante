# Story 1.6 — Health Check Consolidado & Smoke Test

| Campo | Valor |
|-------|-------|
| **Epic** | EPIC-1: Foundation & Data Layer |
| **Status** | Ready |
| **Agente Executor** | @dev (Dex) |
| **QA Gate** | @architect (Aria) |
| **Quality Gate Tools** | `["lint", "typecheck", "unit-tests", "e2e-smoke-tests"]` |
| **Wave** | 4 (Sequential — validação final) |
| **Complexidade** | S (~2-4h) |
| **Prioridade** | P0-critical |
| **Branch** | `feat/1.6-health-smoke-test` |

---

## Descrição

Consolidar o health check para retornar status de todas as dependências (database, redis, vectordb, hotels_count) e criar smoke test suite que valida o funcionamento end-to-end de todo o EPIC-1. Atualizar CI para incluir smoke tests.

**Problema/Necessidade:** Após todas as stories de fundação, precisamos de uma validação end-to-end que confirme que todos os componentes estão funcionando em conjunto. É o quality gate final do EPIC-1.

---

## Acceptance Criteria

- [ ] AC1: `GET /health` retorna status consolidado (database, redis, vectordb, hotels_count)
- [ ] AC2: Status `degraded` se qualquer dep falha (não 500)
- [ ] AC3: `GET /health/ready` (readiness probe para Docker)
- [ ] AC4: `GET /health/live` (liveness probe para Docker)
- [ ] AC5: Smoke test suite: health 200, 92 hotéis, embeddings exist, Redis OK
- [ ] AC6: CI atualizado para rodar smoke tests

---

## Scope

### IN
- Health check consolidado com todas as dependências
- Readiness e liveness probes
- Smoke test suite (e2e)
- CI pipeline atualizado

### OUT
- Novos endpoints de API (epic futuro)
- Load testing (epic futuro)
- Monitoring dashboards (epic futuro)

---

## Tasks / Subtasks

- [ ] **Fase 0: Pré-requisitos**
  - [ ] Confirmar Stories 1.2, 1.3, 1.4, 1.5 concluídas
  - [ ] Todas as dependências (database, redis, hotels, embeddings) operacionais
- [ ] **T1: Health Check Consolidado** (AC1, AC2)
  - [ ] Consolidar `backend/src/api/health.ts` com status de todas deps
  - [ ] Retornar: `{ database, redis, vectordb, hotels_count }`
  - [ ] Status `degraded` se qualquer dep falha (não 500, não error)
- [ ] **T2: Probes Docker** (AC3, AC4)
  - [ ] Criar `GET /health/ready` (readiness probe) — retorna 503 se degraded
  - [ ] Criar `GET /health/live` (liveness probe) — sempre 200 se processo vivo
- [ ] **T3: Smoke Test Suite** (AC5)
  - [ ] Criar `tests/e2e/smoke-test.test.ts`
  - [ ] Test: health endpoint retorna 200
  - [ ] Test: 92 hotéis no database
  - [ ] Test: embeddings existem no pgvector
  - [ ] Test: Redis conectado e respondendo
- [ ] **T4: CI Update** (AC6)
  - [ ] Atualizar `.github/workflows/ci.yml` para rodar smoke tests
  - [ ] Garantir docker-compose up antes dos smoke tests no CI
- [ ] **T5: Validação final**
  - [ ] Smoke tests 100% verde
  - [ ] Health check consolidado 200 com todas deps
  - [ ] CI pipeline completo verde

---

## Files

### To Modify
- `backend/src/api/health.ts` — consolidar todos os checkers
- `.github/workflows/ci.yml` — adicionar smoke tests

### To Create
- `tests/e2e/smoke-test.test.ts`

---

## Dependencies

| Tipo | Dependência | Status |
|------|-------------|--------|
| Blocked by | 1.2 (database), 1.3 (hotels data), 1.4 (FAQ embeddings), 1.5 (Redis) | Pending |

---

## Risks

| Risco | Probabilidade | Impacto | Mitigação |
|-------|---------------|---------|-----------|
| Smoke tests requerem infra completa (DB + Redis + embeddings) | Certa | Média | CI precisa de docker-compose up para testes |
| Flakiness em testes de integração | Média | Média | Retry logic nos testes, timeouts adequados |

---

## Dev Notes

- Esta story é a validação final do EPIC-1
- `GET /health` deve retornar JSON com status individual de cada dependência
- Se qualquer dependência falha, status geral = `degraded` (não `error`)
- Readiness probe retorna 503 se degraded, liveness sempre 200 se processo vivo

---

## Change Log

| Data | Agente | Mudança |
|------|--------|---------|
| 2026-04-02 | @sm (River) | Story criada a partir de EPIC-1-EXECUTION.yaml |
| 2026-04-02 | @sm (River) | Refinement: QA Gate → @architect, quality_gate_tools adicionado, Tasks/Subtasks adicionados (PO validation fix) |
