# Story 2.7 — Session State & Context Persistence

| Campo | Valor |
|-------|-------|
| **Epic** | EPIC-2: Stella Core — Pipeline Multi-Agente |
| **Status** | Ready for Review |
| **Agente Executor** | @dev (Dex) |
| **QA Gate** | @architect (Aria) |
| **Quality Gate Tools** | `["lint", "typecheck", "unit-tests"]` |
| **Wave** | 4 (Parallel com 2.6) |
| **Complexidade** | S (~2-4h) |
| **Prioridade** | P0-critical |
| **Branch** | `feat/2.7-session-context` |
| **FR Coverage** | FR7 |
| **Agent Model Used** | claude-opus-4-6 |

---

## Descrição

Integrar o Session Manager (Story 1.5) com o pipeline de agentes para manter contexto de conversação entre mensagens. Inclui hotelFocus tracking, conversation history com sliding window, e preferências acumuladas. Session expira em 24h com snapshot salvo em conversations.

**Problema/Necessidade:** Sem persistence de contexto, cada mensagem é tratada isoladamente. O guest não pode fazer follow-ups como "e esse hotel tem spa?" sem repetir o nome do hotel.

---

## Acceptance Criteria

- [ ] AC1: Session Manager integrado com pipeline
- [ ] AC2: `hotelFocus` atualizado automaticamente quando guest menciona hotel
- [ ] AC3: Follow-up sem hotel usa `hotelFocus` da sessão
- [ ] AC4: `conversationHistory` max 20 msgs (sliding window)
- [ ] AC5: Preferências acumuladas ao longo da conversa
- [ ] AC6: Session TTL 24h, snapshot em `conversations` ao expirar
- [ ] AC7: Testes: context persistence, hotel switching

---

## Scope

### IN
- Pipeline ↔ Session Manager integration
- hotelFocus auto-tracking
- Conversation history sliding window
- Preferences accumulation
- Session expiry + snapshot
- Testes unitários

### OUT
- Cross-device session (futuro)
- Session analytics (futuro)
- Conversation export (futuro)

---

## Tasks / Subtasks

- [x] **T1: Pipeline Integration** (AC1)
  - [x] Atualizar `backend/src/state/session-manager.ts` com métodos de contexto
  - [x] `getSessionContext(sessionId)` — retorna contexto para pipeline
  - [x] `updateSessionContext(sessionId, { hotelFocus, history, preferences })`
- [x] **T2: Hotel Focus** (AC2, AC3)
  - [x] Detectar hotel mencionado nos tool results
  - [x] Atualizar hotelFocus automaticamente
  - [x] Follow-up sem hotel → usar hotelFocus atual
- [x] **T3: Conversation History** (AC4, AC5)
  - [x] Sliding window de 20 mensagens
  - [x] Acumular preferências (região, pet, pool, etc.)
- [x] **T4: Session Expiry** (AC6)
  - [x] TTL 24h no Redis
  - [x] Ao expirar: salvar snapshot em `conversations` table
- [x] **T5: Testes** (AC7)
  - [x] Criar `tests/unit/session-context.test.ts`
  - [x] Test: context schema, sliding window logic, preference accumulation

---

## Files

### Created
- `tests/unit/session-context.test.ts` — 32 testes (context schema, sliding window, hotel focus, preferences, snapshot)
- `backend/src/state/session-snapshot.ts` — Persistência async de snapshot em conversations table

### Modified
- `backend/src/state/session-manager.ts` — SessionContext interface, getSessionContext, updateSessionContext, addConversationMessage, deleteSessionContext, createEmptyContext
- `backend/src/state/keys.ts` — Adicionado Redis key `sessionCtx`
- `backend/src/agents/pipeline.ts` — Integração com session context, extractHotelFocus via structuredToolOutputs, F2 fix (redundant GET)
- `backend/src/agents/orchestrator.ts` — OrchestratorResult com toolOutputs coletados de RunResult.newItems (F1 fix)
- `tests/unit/pipeline.test.ts` — Mocks atualizados para OrchestratorResult + 4 testes E2E de hotelFocus

---

## Dependencies

| Tipo | Dependência | Status |
|------|-------------|--------|
| Blocked by | 2.1 (SDK setup) | ✅ Done |
| Blocked by | 1.5 (Redis/session) | ✅ Done |

---

## Risks

| Risco | Probabilidade | Impacto | Mitigação |
|-------|---------------|---------|-----------|
| Session data too large for Redis | Baixa | Média | Sliding window + max field sizes |
| Race condition em updates concorrentes | Baixa | Média | Redis atomic operations |

---

## Dev Notes

- Usar session-manager.ts existente (Story 1.5) — apenas adicionar métodos de contexto
- hotelFocus: extrair do resultado de searchHotels ou queryKnowledgeBase
- Sliding window: ao adicionar msg 21, remover msg 1
- Snapshot: ao expirar TTL, trigger salva em PostgreSQL conversations table

---

## Dev Agent Record

### Debug Log
- Pipeline tests falhavam por falta de mocks para session-manager e session-snapshot (adicionados)
- session-context.test.ts: TS strict mode exigiu `!` para array access e `['key']` para index signatures
- **F1 Analise Arquitetural:** 5 opcoes avaliadas (A: tools gravam direto — rejeitada por acoplamento; B: SDK internals — base para E; C: orchestrator metadata — quase; D: event bus — rejeitada por complexidade; **E: collect tool outputs via RunResult.newItems — ESCOLHIDA**). RunResult.newItems contem RunToolCallOutputItem com `.output` estruturado e `.rawItem.name` para identificar a tool. Zero acoplamento, API oficial do SDK, dados reais.
- **F2:** Eliminado `getSessionContext` redundante, reutilizando retorno de `addConversationMessage`

### Completion Notes
- SessionContext armazenado em Redis key `session_ctx:{id}` com TTL 24h (separado do session principal)
- hotelFocus extraido de `RunResult.newItems` → `RunToolCallOutputItem.output` (dados estruturados reais das tools)
- `runOrchestrator` agora retorna `OrchestratorResult { output: string, toolOutputs: Record<string, unknown> }`
- `collectToolOutputs()` filtra `tool_call_output_item` do tipo `function_call_result` — zero dependencia em SDK internals
- Preferences extraidas via regex patterns (petFriendly, poolHeated, region, bradescoCoupon)
- Sliding window: ao adicionar msg 21, slice remove a mais antiga
- Snapshot salvo async (fire-and-forget) no conversations table via upsert apos cada interacao
- F2 fix: snapshot usa retorno direto de `addConversationMessage` (eliminado 1 Redis GET por request)
- 36 testes unitarios (32 session-context + 4 pipeline E2E), 0 regressoes (698/698 unit tests passando)

---

## Change Log

| Data | Agente | Mudança |
|------|--------|---------|
| 2026-04-05 | @sm (River) | Story criada — EPIC-2 Wave 4 |
| 2026-04-05 | @po (Pax) | Validação: GO (10/10). Draft → Ready |
| 2026-04-06 | @dev (Dex) | Implementação completa — T1-T5 done, 32 testes, 0 regressões |
| 2026-04-06 | @qa (Quinn) | Review: CONCERNS — F1 (extractHotelFocus ineficaz), F2 (Redis GET redundante) |
| 2026-04-06 | @dev (Dex) | QA fixes: F1 (OrchestratorResult + RunResult.newItems) + F2 (reuse addConversationMessage return). +4 testes E2E, 698/698 passando |
